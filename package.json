{
  "name": "tcr-kata",
  "scripts": {
    "start": "node index",
    "test": "vitest run",
    "tcr": "(npm test && git add --all && git commit -m WIP || git reset --hard && git clean -df)"
  },
  "private": true,
  "devDependencies": {
    "vitest": "^1.4.0"
  }
}
